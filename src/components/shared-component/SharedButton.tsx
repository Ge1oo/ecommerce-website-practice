const sharedButton = () => {
  return (
    <button className="capitalize cursor-pointer bg-blue-500 hover:bg-transparent hover:text-blue-500 border border-blue-500 py-2 px-5 rounded-md transition-all duration-500 ease-out">
      book a meeting
    </button>
  );
};

export default sharedButton;
